<?xml version="1.0" encoding="utf-8"?>
<!-- Local development only: allow HTTP to emulator / LAN backends. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
